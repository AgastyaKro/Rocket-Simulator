#pragma once

constexpr double GRAVITY = 9.81;

// Default sea-level constants
const double DEFAULT_ENV_TEMP     = 288.15;  // Kelvin
const double DEFAULT_ENV_PRESSURE = 101325;  // Pascals
const double DEFAULT_ENV_ALTITUDE = 0.0;     // Meters
const double DEFAULT_ENV_DENSITY  = 1.225;   // kg/m³
const double M = 0.0289644;      // kg/mol (dry air)
const double R = 8.3144598;      // J/mol·K
const double R_specific = 287.05;         // Specific gas constant for dry air [J/kg·K]
const double LAPSE_RATE = 0.0065;              // K/m (standard lapse rate)
const double MOLAR_MASS = 0.0289644;           // dry air - kg/mol



